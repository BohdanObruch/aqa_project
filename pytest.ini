[pytest]
pythonpath = .
testpaths = tests
addopts =
    -ra
    --alluredir=allure-results
markers =
    component: component-level tests for clients and internal integrations
    e2e: end-to-end API tests
    unit: unit-level tests
